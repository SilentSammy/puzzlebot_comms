push: scp -r .\cam_navigation puzzlebot@192.168.43.125:/home/puzzlebot/ros2_ws/src
pull: scp -r puzzlebot@192.168.43.125:/home/puzzlebot/ros2_ws/src/cam_navigation .\

+ move algorithms to visual_navigation.py
+ fix direction detection issue
+ allow passing a dedicated drawing frame
+ navigate_track
+ allow passing a decision function to navigate_track
+ reduce follow_line throttle authority according to stoplight, and keep resetting intersection stability timer when stoplight is green
+ allow boolean polling of controller axes
+ calibrate camera
+ finish implementing adaptive threshold in follow_line
+ make simulated track more noisy (seams and shadows)
+ implement find_corresponding_point to detect persistent line candidate. for this, make add function called get_persistent_line() to the pipeline
-- make follow_line speed proportional not only to position on screen but also angle
-- smarter intersection alignment algorithm
-- allow streaming estimated pose
+ return 0.0, 0.0 instead of 0,0
+ separate videoplayer and visual_navigation
+ don't use undistorted frame for estimate_flag_distance
+ make non-blocking flag distance estimator
+ allow videoplayer to either take a path to video or images OR Cap object for live layer viewing
+ rename flag_detector package
+ allow resetting everything that persists in visual_navigation
+ allow follow_line_w_signs to do something when done
+ check which videos and screenshots are worth keeping
-- use undistorted image for get_flag_distance, then calculate redistorted corners
+ allow get_flag_distance_nb to draw corners in near-real time
-- cache undistortion maps
-- make identify_stoplight_nb

+ perhaps each algorithm should be a class? With methods to show the vision pipeline, and object level variables instead of pseudo-statics. but how would that work with algorithms that share vision stages?
+ make LineFollower.adaptive_thres
+ add parameters in inits
+ put end_reached in FlagDetector instead
+ add authority to LineFollower, so it displays the correct values in the drawing frame
+ make stoplight detector store a list of booleans
+ make a dedicated class or function for moving to relative waypoints. that class could internally choose whether to use open or closed loop (with odom) depending on the information that's available.
+- make SignDetector that works similar to StoplightDetector
- make a get_persistent_intersection(). It should color an intersection green only when it has been confirmed.
-- make generic algs for location persistence (like in LineFollower) and state persistence (like in StoplightDetector)
- make K and D not globals
-- make pipeline more flexible by allowing steps to either take a frame, or the data from the previous frame
-- create generic LocationalPersistor class which holds a list of persistent_objs, and receives new_objs.
+- batch classify rois
+ custom drawDetections
+ make generic nb polling method / class. make existing nb functions call it. It should allow multiple concurrent threads.
+- wrap TrackNavigator (possibly rename or refactor such that it only handles intersection logic) in new class, that uses ML to decide turn, and stoplight detection to decide the authority of the internal LineFollower.
+- organize modules into packages before I go insane
+ add signs to track
+ test TrackNavigator just with turn signs
+ ignore arrows when navigating, ignore signs when at intersection (although I ended up getting all confirmed signs and using ids 0-3 in the decision func )
+ add a set_sign_distances intermediate step before get_confirmed_signs. add the field to Sign.
+ slow down when roadwork sign is at 50cm or less
+ display authority on frame as percentage if not 1.0
+ slow down permanently until decision func is polled when seeing yield sign
-- add single_actions and ongoing_actions to run when seeing a certain signs
-- evaluate possibilty of doing a little wiggle dance at the end (maybe provide an single_endaction and an ongoing_endaction. Start a sequence in the single and return the output in ongoing)
+ integrate stoplight detection, in a way that can be easily disabled
- StoplightDetectorV2